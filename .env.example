# ============================================================================
# Μικροί Μαθητές - Environment Variables Template
# ============================================================================
# Copy this file to .env.local and fill in your actual values
# cp .env.example .env.local
# ============================================================================

# ----------------------------------------------------------------------------
# SANITY CMS (Required)
# ----------------------------------------------------------------------------
# Get these from: https://sanity.io/manage → Your Project → API
NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-03-01

# Optional: Read token for accessing drafts
SANITY_READ_TOKEN=your-read-token

# Required: Write token for creating content (admin features)
# Get from: Sanity Manage → API → Tokens → Create token (Editor role)
SANITY_TOKEN=your-write-token
# Alternative name (either SANITY_TOKEN or SANITY_WRITE_TOKEN works)
SANITY_WRITE_TOKEN=your-write-token

# Required: Secret for webhook revalidation (generate random string)
# Generate: openssl rand -base64 32
SANITY_REVALIDATE_SECRET=your-revalidation-secret

# ----------------------------------------------------------------------------
# SUPABASE DATABASE (Required)
# ----------------------------------------------------------------------------
# Get these from: https://supabase.com/dashboard → Your Project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ----------------------------------------------------------------------------
# NEXTAUTH AUTHENTICATION (Required for Admin)
# ----------------------------------------------------------------------------
# Generate secret: openssl rand -base64 32
# Or use: https://generate-secret.vercel.app/32
NEXTAUTH_SECRET=your-nextauth-secret

# URL where your app is hosted
NEXTAUTH_URL=http://localhost:3000
# For production: NEXTAUTH_URL=https://yourdomain.com

# ----------------------------------------------------------------------------
# EMAIL SERVICE (Resend) - Required
# ----------------------------------------------------------------------------
# Get from: https://resend.com/api-keys
RESEND_API_KEY=re_your-resend-api-key

# Email address for admin notifications
ADMIN_EMAIL=admin@example.com

# Optional: Custom from email (defaults to noreply@mikroimathites.gr)
RESEND_FROM_EMAIL=Mikroi Mathites <noreply@mikroimathites.gr>

# Optional: Resend account email
RESEND_ACCOUNT_EMAIL=mikrimathites@outlook.com

# ----------------------------------------------------------------------------
# ANALYTICS (Optional)
# ----------------------------------------------------------------------------
# Google Analytics 4 - Get from: https://analytics.google.com
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Google AdSense - Get from: https://www.google.com/adsense
NEXT_PUBLIC_ADSENSE_CLIENT=ca-pub-XXXXXXXXXX

# ----------------------------------------------------------------------------
# SITE CONFIGURATION (Optional)
# ----------------------------------------------------------------------------
# Production site URL (used for canonical URLs, email links, etc.)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# For production: NEXT_PUBLIC_SITE_URL=https://yourdomain.com

# ============================================================================
# NOTES:
# ============================================================================
# 1. Never commit .env.local to git (it's in .gitignore)
# 2. NEXT_PUBLIC_* variables are exposed to the browser (safe for public values)
# 3. Variables without NEXT_PUBLIC_ are server-only (keep secrets here)
# 4. For production, set these in your hosting platform's environment variables
# ============================================================================
